#pragma once
#include "PTS.h"
#include <string>

void ViewText(const std::string[], const int);
void BackToMenu();
void MainMenu();
void ViewMenu();
void EditMenu(PTS::GroupType group);
void SearchMenu();
